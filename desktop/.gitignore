node_modules/
dist/
cookies/
.env.local
release/

# Build outputs from scripts/build-remote.js (server-deployed artifacts).
# Source-of-truth lives under src/. Only version.json and the legacy
# webContents-injected scripts/ folder are committed.
remote-code/main/
remote-code/renderer/
